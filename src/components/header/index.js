import React from "react";
import { Card } from "@material-ui/core";

const Header = (props) => {
  return (
    <header>
      <Card>Our Store</Card>
    </header>
  );
};

export default Header;
